fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
pgvector==0.5.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
pypdf==5.1.0
python-multipart==0.0.20
websockets==13.1
httpx==0.28.1
openai==3.10.0
numpy==2.2.6

pytest==8.3.4
pytest-asyncio==0.25.0

# --- Optional: LLM_PROVIDER=sarvam_m only ---
# Not installed by default — most deployments use LLM_PROVIDER=gemini. Run
# scripts/check_sarvam_hardware.py FIRST; only install this if it reports
# SUPPORTED or MARGINAL, otherwise it will install but the provider will
# refuse to load with "UNSUPPORTED HARDWARE".
# llama-cpp-python==0.3.5
